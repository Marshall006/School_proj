# Copiez en `web/.env.local`.
#
# Next.js lit le `.env` de CE dossier, pas celui de la racine du depot.
# Cette variable est figee au moment du build : apres l'avoir changee,
# relancez `npm run dev` (ou refaites `npm run build`).

NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
